# excel-mcp-server (Docker Compose) environment variables
#
# Usage:
#   cd py
#   cp .env.example .env
#   # edit .env
#   docker compose up -d --build

# Required: Public base URL reachable by end users (browser).
# This is used to build `download_url` returned by MCP tools.
#
# Examples:
# - Localhost (host machine browser): http://localhost:8585
# - Remote server: https://excel-mcp.example.com
EXCEL_PUBLIC_BASE_URL=http://localhost:8585

# Optional: directory inside the container that is exposed as `/files/...`
# Default in server code: /mnt/data
EXCEL_SHARED_DIR=/mnt/data

# Optional: host-side directory to mount into the container as /mnt/data
# Default in docker-compose.yml: ./shared (relative to py/)
MCP_SHARED_DIR=./shared